#include <iostream>
#include <string>
#include <fstream>
#include <set>

void day04() {
    std::ifstream file ("input.txt");
    std::string value;
    if (file.is_open())
    while (!file.eof()) {

    }
}